type CapacitorConfig = {
  appId: string;
  appName: string;
  webDir: string;
};

const config: CapacitorConfig = {
  appId: "ai.utopia.video",
  appName: "Nemesis Videos AI",
  webDir: ".next",
};

export default config;
